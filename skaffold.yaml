apiVersion: skaffold/v1beta8
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/nexenta/edgefs-csi:{{.VERSION}}'
  artifacts:
  - image: nexenta/edgefs-csi
    context: .
    docker:
      dockerfile: Dockerfile
      buildArgs:
        EDGEFS_IMAGE: 10.105.115.0:5000/nexenta/edgefs:1.1.25
